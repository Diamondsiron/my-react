import React from  'react'

function IndexTitle(props){
    
    return (
        <div className='index-title'>
            <div>{props.title}</div>        
        </div>
    )
}

export default IndexTitle